export enum NameSpaceToken {
  Jwt = 'jwt',
  App = 'app',
  Rsa = 'rsa',
}

export enum PropertyToken {
  Secret = 'secret',
  Code = 'code',
  PublicKey = 'public-key',
  PrivateKey = 'private-key',
}
